package org.generation.ALMIUX.model;

// ** Enum propio para los roles de usuario definidos en el diagrama ER (columna "rol" en tabla USUARIOS)
public enum UserRole {
    // ** Cliente regular de la tienda
    CLIENTE,

    // ** Administrador con acceso total al sistema
    ADMIN
}
